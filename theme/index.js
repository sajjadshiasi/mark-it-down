const colors = require('./colors');
const fontSize = require('./fontSizes');
const fontWeight = require('./fontWeights');
const screens = require('./screens');
const spacing = require('./spacing');

const extend = {};

module.exports = {
  colors,
  fontSize,
  fontWeight,
  screens,
  extend,
  spacing,
};
